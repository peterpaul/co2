include CompileObject
include Type

/*
void Expression_generate_actual_argument (Expression arg, va_list *app) {
	Bool *is_first_arg = va_arg (*app, Bool *);

	if (!*is_first_arg) {
		fprintf (out, ", ");
	} else {
		*is_first_arg = false;
	}
	arg.generate ();
}
*/

class Expression : CompileObject {
	Type type;

	~Expression () {
		type?.release ();
	}

	void generate () {
		generate_left (true);
	}

	void set_scope (Scope scope);


	void generate_left (Bool left);
}
