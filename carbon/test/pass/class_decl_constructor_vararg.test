["stdio.h"] {
	int vprintf(char *format, va_list ap);
}

class MyObject {
	MyObject(char[] format, ...) {
		vprintf(format, ...);
	}
}

int main() {
	new MyObject("TEST: %s %s\n", "Hello", "World!");
	new MyObject("TAST: \n");
	return 0;
}
